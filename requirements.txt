# Install with the same Python used by the launcher:
# py -3 -m pip install -r requirements.txt
# Standard CPython 3.10-3.14 x64; Python 3.12 is recommended, not required.
# Qt modules and Shiboken are resolved by PySide6. No ML/Zemax backend is required.
PySide6==6.11.2
